import type { LabConfig, LabDevice } from "@/types"

export const lab18Devices: LabDevice[] = [
  {
    id: "leaf-01",
    type: "leaf-switch",
    label: "leaf-01",
    sublabel: "SN5600 - Spectrum-4 ASIC",
    prompt: "cumulus@leaf-01:~$",
    osLabel: "Cumulus Linux 5.x / NVUE",
    allowedCommands: [
      "nv show interface swp1 qos roce status",
      "nv show interface swp1 qos roce status pool-map",
      "nv set qos congestion-control default-global traffic-class 3 min-threshold 500000",
      "nv set qos congestion-control default-global traffic-class 3 max-threshold 1500000",
      "nv config apply",
      "nv config save",
      "nv show interface swp1 qos roce counters",
      "nv show interface swp1 counters qos pfc-stats",
      "help",
      "hint",
    ],
    position: { x: 150, y: 200 },
    status: "up",
  },
  {
    id: "dgx-node-01",
    type: "dgx",
    label: "dgx-node-01",
    sublabel: "DGX H100 - 8x ConnectX-7",
    prompt: "dgx-01:~$",
    osLabel: "Ubuntu 22.04 / MLNX_OFED",
    allowedCommands: [
      "ib_write_bw -d mlx5_0 --iters 10000 --size 65536 192.168.100.2 &",
      "ib_write_bw -d mlx5_1 --iters 10000 --size 65536 192.168.100.2 &",
      "ib_write_bw -d mlx5_2 --iters 10000 --size 65536 192.168.100.2 &",
      "ib_write_bw -d mlx5_3 --iters 10000 --size 65536 192.168.100.2 &",
      "ibv_devinfo -d mlx5_0",
      "help",
      "hint",
    ],
    position: { x: 400, y: 380 },
    status: "up",
  },
]

export const lab18: LabConfig = {
  id: "lab18-ecn-threshold-tuning",
  title: "ECN Threshold Tuning Under Load",
  difficulty: "intermediate",
  scenario:
    "A 128-GPU training job is stuck near 60% throughput and swp1 RoCE counters show zero ECN marks under load. Compare the interface ECN thresholds against the swp1 RoCE pool map, then tune TC3 thresholds so DCQCN starts marking before the queue exhausts its lossless buffer.",
  expectedMinutes: 35,
  initialTopology: {
    pfcEnabled: true,
    ecnEnabled: true,
    roceShorthandApplied: true,
    configApplied: true,
    congestionDetected: true,
    silentCongestion: true,
    bufferUtilPct: 95,
    mtu: 9216,
    ecnMinThreshold: 10485760,
    ecnMaxThreshold: 31457280,
    ecnMarkingActive: false,
    thresholdInspected: false,
    problemIdentified: false,
    thresholdFixed: false,
    ecnMarkingVerified: false,
    losslessBufferBytes: 6291456,
    lab18StartedFlows: [],
  },
  requiredConditions: [
    "thresholdInspected",
    "problemIdentified",
    "thresholdFixed",
    "ecnMarkingVerified",
  ],
  hints: [
    {
      level: 1,
      triggerAfterMistakes: 3,
      triggerAfterSeconds: 120,
      text: "Start by comparing `nv show interface swp1 qos roce status` against `nv show interface swp1 qos roce status pool-map`. If the ECN min threshold is higher than the RoCE egress pool size, marking cannot start in time.",
    },
    {
      level: 2,
      triggerAfterMistakes: 6,
      triggerAfterSeconds: 240,
      text: "Fix TC3's ECN range with `nv set qos congestion-control default-global traffic-class 3 min-threshold 500000` and `nv set qos congestion-control default-global traffic-class 3 max-threshold 1500000`, then `nv config apply`.",
    },
    {
      level: 3,
      triggerAfterMistakes: 10,
      triggerAfterSeconds: 400,
      text: "After applying the fix, start the four `ib_write_bw ... &` streams again and check `nv show interface swp1 qos roce counters`. The `tx-ecn-stats ecn-marked-packets` counter should move above zero while PFC pause pressure drops.",
    },
  ],
}
