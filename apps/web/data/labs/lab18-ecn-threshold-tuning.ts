import type { LabConfig, LabDevice } from "@/types"

export const lab18Devices: LabDevice[] = [
  {
    id: "leaf-01",
    type: "leaf-switch",
    label: "leaf-01",
    sublabel: "SN5600 â€” Spectrum-4 ASIC",
    prompt: "leaf-01#",
    osLabel: "Cumulus Linux 5.x / NVUE",
    allowedCommands: [
      "nv show qos ecn profile roce",
      "cl-resource-query",
      "nv set qos ecn profile roce min-threshold 500000",
      "nv set qos ecn profile roce max-threshold 1500000",
      "nv config apply",
      "nv config save",
      "ethtool -S swp1 | grep ecn",
      "nv show interface swp1 counters",
      "nv show interface swp1 qos",
      "nv show qos trust dscp-map",
    ],
    position: { x: 150, y: 200 },
    status: "up",
  },
  {
    id: "dgx-node-01",
    type: "dgx",
    label: "dgx-node-01",
    sublabel: "DGX H100 â€” 8x ConnectX-7",
    prompt: "dgx-01:~$",
    osLabel: "Ubuntu 22.04 / MLNX_OFED",
    allowedCommands: [
      "ib_write_bw -d mlx5_0 --iters 10000 --size 65536 192.168.100.2 &",
      "ib_write_bw -d mlx5_1 --iters 10000 --size 65536 192.168.100.2 &",
      "ib_write_bw -d mlx5_2 --iters 10000 --size 65536 192.168.100.2 &",
      "ib_write_bw -d mlx5_3 --iters 10000 --size 65536 192.168.100.2 &",
      "ibv_devinfo -d mlx5_0",
    ],
    position: { x: 400, y: 380 },
    status: "up",
  },
]

export const lab18: LabConfig = {
  id: "lab18-ecn-threshold-tuning",
  title: "ECN Threshold Tuning Under Load",
  difficulty: "intermediate",
  scenario: "A 128-GPU training job shows 60% throughput. Investigation reveals DCQCN never triggers (tx_ecn_marked_pkts=0). Root cause: ECN min_threshold set to 10 MB â€” above the 6 MB TC3 lossless buffer. Find and fix.",
  expectedMinutes: 35,
  initialTopology: {
    pfcEnabled: true,
    ecnEnabled: true,
    roceShorthandApplied: true,
    configApplied: true,
    congestionDetected: true,
    silentCongestion: true,
    bufferUtilPct: 95,
    mtu: 9216,
    ecnMinThreshold: 10485760,
    ecnMaxThreshold: 31457280,
    ecnMarkingActive: false,
    thresholdInspected: false,
    problemIdentified: false,
    thresholdFixed: false,
    ecnMarkingVerified: false,
    losslessBufferBytes: 6291456,
  } as any,
  requiredConditions: [
    "thresholdInspected",
    "problemIdentified",
    "thresholdFixed",
    "ecnMarkingVerified",
  ],
  hints: [
    {
      level: 1,
      triggerAfterMistakes: 3,
      triggerAfterSeconds: 120,
      text: "Run `nv show qos ecn profile roce` to see the current thresholds. Then run `cl-resource-query` to see how much buffer TC3 actually has. Compare the two numbers.",
    },
    {
      level: 2,
      triggerAfterMistakes: 6,
      triggerAfterSeconds: 240,
      text: "The ECN min_threshold (10 MB) exceeds the total TC3 lossless buffer (6 MB). Fix: `nv set qos ecn profile roce min-threshold 500000` and `nv set qos ecn profile roce max-threshold 1500000`.",
    },
    {
      level: 3,
      triggerAfterMistakes: 10,
      triggerAfterSeconds: 400,
      text: "After applying the fix, verify by running multi-flow traffic and checking `ethtool -S swp1 | grep ecn` on the leaf switch. You should see tx_ecn_marked_pkts incrementing.",
    },
  ],
}
