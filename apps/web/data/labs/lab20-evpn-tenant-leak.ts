import type { LabConfig, LabDevice } from "@/types"

export const lab20Devices: LabDevice[] = [
  {
    id: "leaf-01",
    type: "leaf-switch",
    label: "leaf-01",
    sublabel: "Spectrum-4 / Cumulus Linux",
    prompt: "cumulus@leaf-01:~$",
    osLabel: "Cumulus Linux 5.x",
    allowedCommands: [
      "net show bgp vrf VRF_A ipv4 unicast",
      "net show bgp evpn route type 5",
      "nv show vrf VRF_A router bgp route-import",
      "nv show vrf VRF_A router bgp route-export",
      "net show bgp vrf VRF_A evpn route",
      "nv show evpn",
      "net show wjh",
      "nv show vxlan",
    ],
    position: { x: 200, y: 200 },
    status: "up",
  },
  {
    id: "leaf-02",
    type: "leaf-switch",
    label: "leaf-02",
    sublabel: "Spectrum-4 / Cumulus Linux",
    prompt: "cumulus@leaf-02:~$",
    osLabel: "Cumulus Linux 5.x",
    allowedCommands: [
      "net show bgp vrf VRF_B ipv4 unicast",
      "net show bgp evpn route type 5",
      "nv show vrf VRF_B router bgp route-import",
      "nv show vrf VRF_B router bgp route-export",
      "nv unset vrf VRF_B router bgp route-import from-evpn route-target 65000:100",
      "nv config apply",
      "net clear bgp vrf VRF_B *",
      "nv show evpn",
      "net show wjh",
    ],
    position: { x: 480, y: 200 },
    status: "up",
  },
  {
    id: "dgx-tenanta",
    type: "dgx",
    label: "dgx-tenantA",
    sublabel: "DGX H200 - TENANT A",
    prompt: "root@dgx-tenanta:~#",
    osLabel: "Ubuntu 22.04",
    allowedCommands: [
      "ping -c 3 10.100.1.5",
      "ib_write_bw -d mlx5_0 -x 3 10.100.1.5 --port 18515 --duration 5",
      "ip route show vrf default",
    ],
    position: { x: 200, y: 380 },
    status: "up",
    railId: 0,
  },
  {
    id: "dgx-tenantb",
    type: "dgx",
    label: "dgx-tenantB",
    sublabel: "DGX H200 - TENANT B",
    prompt: "root@dgx-tenantb:~#",
    osLabel: "Ubuntu 22.04",
    allowedCommands: [
      "ping -c 3 10.100.1.5",
      "ip route show vrf default",
    ],
    position: { x: 480, y: 380 },
    status: "up",
    railId: 1,
  },
]

export const lab20: LabConfig = {
  id: "lab20-evpn-tenant-leak",
  title: "Lab 20 - EVPN Tenant Route Leak",
  difficulty: "advanced",
  scenario:
    "TenantB can reach TenantA's storage subnet 10.100.1.0/24 due to a misconfigured BGP-EVPN "
    + "route-target import on leaf-02. Find the misconfiguration and restore tenant isolation "
    + "without breaking intra-tenant connectivity.",
  expectedMinutes: 45,
  initialTopology: {
    pfcEnabled: true,
    ecnEnabled: true,
    congestionDetected: false,
    silentCongestion: false,
    bufferUtilPct: 12,
    evpnEnabled: true,
    vrfBImportRtCorrect: false,
    vrfBHasLeakedRoute: true,
    leakConfirmed: false,
    leakyLeafIdentified: false,
    rtMisconfigFound: false,
    rtFixed: false,
    isolationRestored: false,
    crossTenantBlocked: false,
    legitimateAccessVerified: false,
    rdmaVerified: false,
    bgpSoftResetDone: false,
  },
  requiredConditions: [
    "leakConfirmed",
    "leakyLeafIdentified",
    "rtMisconfigFound",
    "rtFixed",
    "isolationRestored",
    "crossTenantBlocked",
    "legitimateAccessVerified",
  ],
  hints: [
    {
      level: 1,
      triggerAfterMistakes: 3,
      triggerAfterSeconds: 120,
      text: "Start on leaf-02: run 'net show bgp vrf VRF_B ipv4 unicast' and look for any prefixes in the 10.100.x.x range. These belong to TenantA's storage and should not appear in VRF_B.",
    },
    {
      level: 2,
      triggerAfterMistakes: 6,
      triggerAfterSeconds: 240,
      text: "Run 'nv show vrf VRF_B router bgp route-import' on leaf-02. You should see two route-targets listed: 65000:200 (correct for TenantB) and 65000:100 (incorrect - this is TenantA's RT). Remove the 65000:100 import.",
    },
    {
      level: 3,
      triggerAfterMistakes: 10,
      triggerAfterSeconds: 400,
      text: "Fix command on leaf-02: 'nv unset vrf VRF_B router bgp route-import from-evpn route-target 65000:100' followed by 'nv config apply'. Then rerun 'net show bgp vrf VRF_B ipv4 unicast' and verify TenantB can no longer ping 10.100.1.5.",
    },
  ],
}
