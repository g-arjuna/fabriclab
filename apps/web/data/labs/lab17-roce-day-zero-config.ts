import type { LabConfig, LabDevice } from "@/types"

export const lab17Devices: LabDevice[] = [
  {
    id: "leaf-01",
    type: "leaf-switch",
    label: "leaf-01",
    sublabel: "SN5600 â€” Spectrum-4 ASIC",
    prompt: "leaf-01#",
    osLabel: "Cumulus Linux 5.x / NVUE",
    allowedCommands: [
      "nv set interface swp1-32 qos roce",
      "nv config apply",
      "nv config save",
      "nv show qos roce",
      "nv show interface swp1 qos",
      "nv show qos trust dscp-map",
      "nv show qos ecn profile roce",
      "nv show qos pfc",
      "nv show qos scheduler",
      "nv show interface swp1 counters pfc",
      "cl-resource-query",
      "ethtool -S swp1 | grep ecn",
    ],
    position: { x: 150, y: 200 },
    status: "up",
  },
  {
    id: "leaf-02",
    type: "leaf-switch",
    label: "leaf-02",
    sublabel: "SN5600 â€” Spectrum-4 ASIC",
    prompt: "leaf-02#",
    osLabel: "Cumulus Linux 5.x / NVUE",
    allowedCommands: [
      "nv set interface swp1-32 qos roce",
      "nv config apply",
      "nv config save",
      "nv show qos roce",
      "nv show interface swp1 qos",
      "nv show qos trust dscp-map",
      "nv show qos ecn profile roce",
      "nv show qos pfc",
    ],
    position: { x: 500, y: 200 },
    status: "up",
  },
  {
    id: "dgx-node-01",
    type: "dgx",
    label: "dgx-node-01",
    sublabel: "DGX H100 â€” 8x ConnectX-7",
    prompt: "dgx-01:~$",
    osLabel: "Ubuntu 22.04 / MLNX_OFED",
    allowedCommands: [
      "ib_write_bw -d mlx5_0 --iters 5000 --size 65536 192.168.100.2",
      "ib_write_lat -d mlx5_0 --iters 10000 192.168.100.2",
      "ibv_devinfo -d mlx5_0",
      "ibv_rc_pingpong -d mlx5_0 -g 0 192.168.100.2",
      "ip link show eth0",
      "ethtool -S mlx5_0 | grep -E 'ecn|pfc|retry'",
    ],
    position: { x: 330, y: 380 },
    status: "up",
  },
]

export const lab17: LabConfig = {
  id: "lab17-roce-day-zero-config",
  title: "RoCE Day-Zero Configuration",
  difficulty: "intermediate",
  scenario: "Apply and verify the full RoCE QoS configuration on a Spectrum-X leaf pair for a 32-GPU DGX H100 cluster. Configure DSCP trust, PFC on priority 3, ECN thresholds, and verify with ib_write_bw and ib_write_lat.",
  expectedMinutes: 45,
  initialTopology: {
    pfcEnabled: false,
    ecnEnabled: false,
    congestionDetected: false,
    silentCongestion: false,
    bufferUtilPct: 0,
    roceShorthandApplied: false,
    dscpTrustVerified: false,
    ecnThresholdsVerified: false,
    pfcConfigVerified: false,
    bwVerified: false,
    latencyVerified: false,
    configApplied: false,
    mtu: 9216,
    ecnMinThreshold: 500000,
    ecnMaxThreshold: 1500000,
    ibWriteBwResult: null,
    ibWriteLatP99: null,
  } as any,
  requiredConditions: [
    "roceShorthandApplied",
    "dscpTrustVerified",
    "ecnThresholdsVerified",
    "pfcConfigVerified",
    "bwVerified",
    "latencyVerified",
  ],
  hints: [
    {
      level: 1,
      triggerAfterMistakes: 3,
      triggerAfterSeconds: 120,
      text: "Apply the RoCE shorthand first: `nv set interface swp1-32 qos roce` followed by `nv config apply`. This sets DSCP trust, PFC on priority 3, and ECN thresholds in a single command.",
    },
    {
      level: 2,
      triggerAfterMistakes: 6,
      triggerAfterSeconds: 240,
      text: "Verify each QoS component individually: `nv show qos trust dscp-map` for DSCP mapping, `nv show qos pfc` for PFC priority, and `nv show qos ecn profile roce` for ECN thresholds.",
    },
    {
      level: 3,
      triggerAfterMistakes: 10,
      triggerAfterSeconds: 400,
      text: "If ib_write_bw shows ~12 GB/s instead of ~46 GB/s, the MTU is 1500 bytes. Fix: `ip link set eth0 mtu 9000` on the DGX node and `nv set interface swp1-32 link mtu 9000` on the leaf switch, then `nv config apply`.",
    },
  ],
}
